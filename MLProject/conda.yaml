name: telco-churn-env
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - imbalanced-learn
      - xgboost
      - mlflow
      - dagshub
